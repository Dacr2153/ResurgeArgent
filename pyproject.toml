[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agente-matching"
version = "0.1.0"
description = "Agente 7 - Matching/Asignacion (necesidades, recursos, empresas, vehiculos)"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "networkx>=3.0",
    "anthropic>=0.40",
    "openai>=1.0",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    # Ed25519 para firmar los sobres de la malla P2P en origen: un reporte
    # atraviesa telefonos de desconocidos y no puede ser alterable en transito.
    "cryptography>=42.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-mock>=3.12",
    "ruff>=0.4",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
